// fork
export default function* rootSaga() {
  yield all([
    fork(authSaga),
    fork(componentsSaga),
    fork(i18nSaga),
    fork(materialsSaga),
    fork(offersSaga),
    fork(productsSaga),
    fork(RedirectSaga)
  ]);
}
